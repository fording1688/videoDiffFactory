# Bundled FFmpeg

Put platform-specific FFmpeg binaries here when you want a fully portable package:

- macOS/Linux: `ffmpeg` and `ffprobe`
- Windows: `ffmpeg.exe` and `ffprobe.exe`

The app checks this folder first, then falls back to the system PATH.
